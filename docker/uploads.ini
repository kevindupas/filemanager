; File-manager upload limits — big enough for movie uploads (chunked at 5 MB,
; but generous so any path / future direct uploads work too).
upload_max_filesize = 12G
post_max_size = 12G
memory_limit = 512M
max_execution_time = 0
max_input_time = 0
max_file_uploads = 50
